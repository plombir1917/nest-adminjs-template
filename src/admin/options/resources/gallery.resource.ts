import { ResourceOptions } from 'adminjs';

export const GalleryResourceOptions: ResourceOptions = {
  properties: {
    uploadPhoto: {
      isVisible: true,
    },
    photo: {
      isVisible: false,
    },
  },
};
