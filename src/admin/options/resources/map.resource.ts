import { ResourceOptions } from 'adminjs';

export const MapResourceOptions: ResourceOptions = {
  properties: {
    uploadPhoto: {
      isVisible: true,
    },
    photo: {
      isVisible: false,
    },
  },
};
